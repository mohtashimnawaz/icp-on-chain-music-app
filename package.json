{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "icp-music-platform",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "prebuild": "npm run prebuild --workspaces --if-present",
    "pretest": "npm run prebuild --workspaces --if-present",
    "start": "npm start --workspaces --if-present",
    "test": "npm test --workspaces --if-present",
    "dev": "npm run dev --workspace=src/icp-music-platform-frontend",
    "dev:frontend": "npm run dev --workspace=src/icp-music-platform-frontend",
    "build:frontend": "npm run build --workspace=src/icp-music-platform-frontend"
  },
  "type": "module",
  "workspaces": [
    "src/icp-music-platform-frontend"
  ],
  "dependencies": {
    "@dfinity/principal": "^2.4.1",
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.1",
    "@mui/icons-material": "^7.2.0",
    "@mui/material": "^7.2.0"
  }
}
